## If you are running the newer openssl on your machine, you will need to run the following line in your terminal before running rails s

web: bin/rails server -p 3000
webpack: NODE_OPTIONS=--openssl-legacy-provider bin/webpack-dev-server
